name: Deploy Media Stack

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'services/media-stack/**'
      - '.github/workflows/deploy-media-stack.yaml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run Ansible Playbook
        uses: dawidd6/action-ansible-playbook@v5
        with:
          playbook: playbook-deploy-media-stack.yaml
          directory: ansible
          key: ${{ secrets.CLOUD_KEY }}
          options: |
            -i inventory/hosts.ini
            --limit almonium
